define(['thorax'], function (Thorax) {
  // This is your base View class. All Views in your app should inherit from
  // this module, so feel free to define any app-wide specifics here.
  // (i.e. logged in state, or any data that all views should have access to)
  // You can also subclass this View to serve as a more specific base view
  // for forms or other modules

  return Thorax.View.extend({

  });
});

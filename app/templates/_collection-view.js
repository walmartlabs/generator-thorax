define(['thorax'], function (Thorax) {
  // This is your base Collection class. All Models in your app should inherit from
  // this module, so feel free to define any app-wide specifics here.
  // (i.e. how to animate adding / removing views. You can also subclass your
  // base CollectionView if you need something specific like draggable/reorderable
  // lists)

  return Thorax.CollectionView.extend({

  });
});

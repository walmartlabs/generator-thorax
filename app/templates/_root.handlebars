{{! This template is used by the <%= _.classify(name) %>.root
    view calling <%= _.classify(name) %>.layout.setView(view)
    will place a view where layout-element is called below. }}

{{layout-element}}

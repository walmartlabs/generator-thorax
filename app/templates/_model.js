define(['thorax'], function (Thorax) {
  // This is your base Model class. All Models in your app should inherit from
  // this module, so feel free to define any app-wide specifics here.
  // (i.e. override `url` to combine your API root with a model-specific `urlPath`
  // or `name` paramter that Backbone can use, or automatically extract nested
  // JSON using a `name` parameter)
  return Thorax.Model.extend({

  });
});
